import { Typography } from "@mui/material";

export default function CheckoutPage() {
    return (
        <>
            <Typography variant='h4'>Only logged in users should be able to see this!</Typography>
        </>
    )
}